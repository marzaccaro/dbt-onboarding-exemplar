version: 2

models:
  - name: time_spine_daily
    description: Daily time spine for MetricFlow semantic models.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Calendar date at daily grain.
        granularity: day
